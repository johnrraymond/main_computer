# The core project uses only the Python standard library.
# Optional:
#   pip install openai   # enables --provider openai
#   pip install playwright && python -m playwright install chromium
#     enables the browser widget harness
psutil==7.2.2
podman-compose
Mathics3==10.0.0
tenseal>=0.3.15
PySide6==6.11.0
playwright==1.59.0
pypdf==6.10.2
pytest==9.0.3
detect-secrets==1.5.0
paramiko==5.0.0
eth-account==0.13.7
foundationdb==7.4.6