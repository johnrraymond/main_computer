"""Internal Hub Control implementation boundary.

The repository-level ``hub_mutate_harness.py`` is the normal operator surface.
This package is intentionally internal and will acquire lifecycle implementation
behind the frozen operator contract.
"""
