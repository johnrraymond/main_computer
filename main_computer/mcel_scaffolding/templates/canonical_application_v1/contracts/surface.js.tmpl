export const {{APP_CLASS_NAME}}Surface = Object.freeze({
  schema: "mcel.semantic-surface-ir.v1",
  appId: {{APP_ID_JSON}},
  surfaceId: "{{APP_ID}}.surface.primary",
  regions: Object.freeze([
    Object.freeze({ id: "{{APP_ID}}.region.shell", role: "application" }),
    Object.freeze({ id: "{{APP_ID}}.region.value", role: "result" }),
    Object.freeze({ id: "{{APP_ID}}.region.controls", role: "toolbar" }),
    Object.freeze({ id: "{{APP_ID}}.region.evidence", role: "status" })
  ]),
  nodes: Object.freeze([
    Object.freeze({ id: "{{APP_ID}}.value", kind: "state-value", statePath: "count", regionId: "{{APP_ID}}.region.value" }),
    Object.freeze({ id: "{{APP_ID}}.increment-control", kind: "control", intentId: "increment", regionId: "{{APP_ID}}.region.controls" }),
    Object.freeze({ id: "{{APP_ID}}.reset-control", kind: "control", intentId: "reset", regionId: "{{APP_ID}}.region.controls" }),
    Object.freeze({ id: "{{APP_ID}}.latest-receipt", kind: "operation-evidence", regionId: "{{APP_ID}}.region.evidence" })
  ])
});
