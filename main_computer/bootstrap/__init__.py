"""Python-owned Windows bootstrap support for Main Computer.

These modules are intentionally dependency-light so the managed CPython stage can
run them before the project package and optional dependencies are installed.
"""
